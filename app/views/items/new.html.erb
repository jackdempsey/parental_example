<h1>New item</h1>

<% form_for(@item) do |f| %>
  <%= f.error_messages %>
  <% f.fields_for :product do |product| %>
    <%= product.text_field :name %>
  <% end %>
  <p>
    <%= f.submit 'Create' %>
  </p>
<% end %>

<%= link_to 'Back', items_path %>
